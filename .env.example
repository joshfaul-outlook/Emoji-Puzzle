# Copy these values to api/local.settings.json for local Azure Functions.
TABLE_STORAGE_CONNECTION_STRING=UseDevelopmentStorage=true
ADMIN_PASSWORD=change-me
ADMIN_SESSION_SECRET=replace-with-at-least-32-random-characters

# Set during production builds so social preview URLs use the deployed origin.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
